# AGENTS.md

本文件定义 AI 编码 Agent 在本项目仓库中的工作方式、代码边界和完成标准。

产品需求、技术方案、数据库设计及实施细节由项目文档维护，本文件不重复记录具体业务内容。

本文件默认适用于整个仓库。子目录中的 `AGENTS.override.md` 或更具体的 `AGENTS.md` 可以为对应目录补充或覆盖局部规则。

## 1. 基本行为

* 始终使用中文回复。
* 代码标识符、API、数据库字段、配置项和 Git 提交信息使用英文。
* 代码注释沿用当前项目已有语言和风格，不批量修改既有注释。
* 实现前先检查相关项目文档、现有代码和测试。
* 文档或现有代码已经能够回答的问题，不重复询问用户。
* 只修改当前任务直接涉及的内容，不进行无关重构、批量格式化或顺手修复。
* 不为尚未明确的未来需求提前建设复杂抽象。
* 当前任务无法完成的部分必须明确说明原因、影响和剩余工作，不承诺后台交付。

## 2. 指令与文档优先级

在不违反平台、安全和权限约束的前提下，项目内信息冲突按以下顺序处理：

1. 用户本次明确要求。
2. 当前作用域内更具体的 `AGENTS.override.md` 或 `AGENTS.md`。
3. 仓库根目录的 `AGENTS.md`。
4. PRD。
5. 技术设计文档。
6. 实施文档。
7. 进度文档。
8. 现有代码和测试。

不同文档分别承担以下职责：

* PRD：定义做什么、用户场景和验收标准。
* 技术设计文档：定义系统如何设计、模块边界和技术约束。
* 实施文档：定义开发步骤、迁移方式和交付顺序。
* 进度文档：记录当前状态、已完成事项、阻塞和下一任务。
* 现有代码和测试：反映当前已经实现的行为。

发现代码、测试和文档不一致时：

1. 不得静默猜测。
2. 根据高优先级信息确认目标行为。
3. 在回复中明确说明差异。
4. 仅在当前任务范围内同步修正相关代码、测试或文档。

## 3. 开始任务前的检查

对于功能实现、缺陷修复、重构、数据库迁移和跨模块变更，开始编码前必须：

1. 查看进度文档，确认当前阶段、下一任务、前置依赖和阻塞。
2. 在 PRD 中搜索相关页面、功能名称、业务规则和验收标准。
3. 在技术设计文档中搜索相关模块、数据库表、API、外部依赖和技术约束。
4. 检查受影响的代码、数据库迁移和测试。
5. 查找仓库中的相似实现，并沿用已有模式。
6. 确认当前变更是否会影响兼容性、数据安全、部署或回滚。

对于仅涉及文案、格式、注释或局部配置的小型任务，只需检查直接相关文件，不强制扫描全部项目文档。

## 4. 开发工作流

* 优先按最小可验证纵向切片完成任务。

* 只处理当前任务实际涉及的层次，不适用的环节直接跳过。

* 一般处理顺序为：

  1. 定位相关文档和现有实现。
  2. 明确验收条件和影响范围。
  3. 修改数据模型或数据库迁移。
  4. 修改后端服务和领域逻辑。
  5. 修改 API。
  6. 修改 UI。
  7. 补充或更新测试。
  8. 更新相关文档和进度。

* 数据库结构发生变化时，先设计迁移、约束和回滚策略，再修改 ORM、服务和 API。

* 需要改变产品范围、系统架构、数据库表结构或实施顺序时，先同步对应文档，再进行编码。

* 一次优先交付一个可以独立验证的闭环，不并行铺开大量未完成实现。

* 不以绕过测试、删除校验或降低约束的方式让任务表面通过。

## 5. 代码结构与依赖方向

* `domain/` 不得依赖具体 Web 框架、数据库实现或第三方 SDK。
* API 层不得直接编写业务 SQL。
* API 层不得直接访问第三方数据源或外部 Provider。
* 外部 Provider 调用应封装在基础设施层或适配器层。
* 配置必须通过项目统一配置机制加载，不在业务代码中分散读取环境变量。
* 新代码应沿用仓库现有目录结构、命名方式、错误处理、日志和依赖注入模式。
* 未经充分理由，不引入新的框架、生产依赖或重复实现已有能力。

## 6. 测试与完成标准

任务完成前必须：

* 运行与变更直接相关的测试、类型检查、静态检查或构建命令。
* 验证最终行为符合用户要求和相关验收标准。
* 检查变更是否引入明显回归、兼容性问题或安全风险。
* 检查最终 diff，确保不包含无关修改。

数据库发生变化时，还必须确认：

* 数据库迁移可以在空库执行。
* 数据库迁移可以从上一版本升级。
* 必要时提供回滚或兼容方案。
* 数据约束、索引、默认值和空值策略已经明确。

涉及外部 Provider 时，还必须处理：

* 请求失败。
* 连接超时和读取超时。
* 空数据。
* 非法响应。
* 过期数据。
* 重试和幂等性。
* Provider 不可用时的降级行为。

无法执行某项验证时，必须说明：

* 未执行的验证项目。
* 无法执行的具体原因。
* 可能存在的风险。
* 建议的人工验证方式。

“代码能够运行”不等于任务完成。测试、文档和进度未按要求同步时，任务仍视为未完成。

## 7. 文档与进度同步

* 代码行为发生变化时，检查对应 PRD、技术设计和实施文档是否需要更新。
* 完成任务后，更新进度文档中的任务状态、实现证据、验证结果、遗留问题和下一任务。
* 文档只更新与当前任务直接相关的部分。
* 不复制大量代码到项目文档中。
* `AGENTS.md` 只维护 Agent 行为、工程约束和完成标准，不复制具体业务需求或数据库字段定义。

## 8. Git 与发布约束

* 未经用户明确授权，不推送远程仓库、不合并分支、不创建正式发布版本。

* 未经用户明确授权，不执行生产部署或生产数据库变更。

* Git 提交只能包含当前任务相关修改。

* 不混入无关格式化、文件移动、依赖升级或重构。

* 提交前检查暂存区和最终 diff。

* 不提交以下内容：

  * `.env*`
  * 密钥、Token、证书和凭据
  * 本地数据库文件
  * 数据库备份
  * 日志文件
  * 依赖目录
  * 构建产物
  * 临时文件
  * 编辑器或操作系统产生的无关文件

* 涉及删除数据、不可逆迁移、强制推送、覆盖远程历史或生产操作时，必须先获得用户明确授权。

## 9. 完成后的回复要求

完成任务后，回复中应明确说明：

1. 已完成的内容。
2. 修改的主要文件。
3. 执行的验证或测试命令。
4. 测试和验证结果。
5. 未完成或未验证的部分及原因。
6. 已知风险和遗留问题。
7. 文档及进度是否已经同步。
8. 建议的下一步任务。

不得声称未实际执行的测试已经通过，也不得隐瞒失败、跳过项或环境限制。
