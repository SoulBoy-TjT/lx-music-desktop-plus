# 音乐目录上下文

本上下文描述从在线音乐平台识别歌手、取得其专辑目录、生成洛雪本地歌单以及整理和转换本地歌曲时使用的统一业务语言。

## Language

**歌手名称输入**：
用户在四平台专辑抓取中一次提供的名称文本；去除首尾空白后作为本批次的共同查询词和四个歌单的名称前缀，但它本身不是任何平台已经确认的歌手身份。
_Avoid_：跨平台唯一歌手、已确认歌手 ID

**歌手引用**：
由音乐来源与该来源中的歌手唯一标识共同组成；一个批次必须用同一个歌手名称分别解析出每个已选来源内引用，不能在平台之间复用 ID。
_Avoid_：仅凭名称认定身份、跨平台歌手 ID

**歌手身份匹配**：
已选来源中，规范化完整展示名与输入相等的 `exact`；网易还允许双脚本组合展示名的 `alias_exact`：完整连续汉字段与完整拉丁字母段相邻组成名称，输入与其中一个完整脚本段相等。adapter 按 Provider 原始稳定顺序选择首个合法结果；网易不全局优先 `exact`。统一确认展示该结果的 Provider 原名和 ID。严格匹配仍只建立待确认关系，不等于系统已经证明主体相同。
_Avoid_：任意 contains、未闭合前后缀、编辑距离、拼音推断、按专辑数自动选人、全局 exact 优先

**抓取来源选择**：
用户点击获取前从 `kg`、`tx`、`wy`、`kw` 中选出的一至四个有序来源；空页面默认全选，零来源阻断且不请求 Provider。点击获取后固化为该 `batchId` 的 `requestedSources`，未选来源不形成空快照或失败。
_Avoid_：固定四来源、未选来源失败、把新来源拼接进旧批次

**四来源抓取批次**：
以一次歌手名称输入和非空 `requestedSources` 为起点，对所选来源并行解析身份并等待全部 settled；只对成功身份非空子集统一确认和抓取，解析失败来源保留 `failed`，零个成功身份不弹确认。所选来源共享取消和重试生命周期，但不共享身份、专辑或歌曲集合。名称中的“四来源”表示能力上限，不要求每批固定请求四个平台。
_Avoid_：跨平台合并目录、四来源同步歌单

**歌手专辑目录**：
指定歌手引用在一个音乐来源中关联的专辑，以及这些专辑详情返回的完整曲目集合；它不代表跨平台绝对完整的作品全集。
_Avoid_：歌手全部歌曲、全网全集

**完整专辑曲目**：
一个专辑版本的详情结果所包含的全部曲目，不限于把目标歌手列为曲目艺人的歌曲。
_Avoid_：歌手歌曲搜索结果、按歌手过滤后的专辑歌曲

**专辑详情稳定曲序**：
优先使用 Provider 的可靠一基专辑曲序并保留其中间缺口；完整详情明确按响应位置返回连续零基序列 `0..N-1` 时，将整张专辑统一平移为 `1..N`。只有没有独立可靠曲序时才使用原始详情位置，不能按成功映射后的剩余歌曲重新压缩编号。
_Avoid_：把单个 `0` 直接丢弃、只修正零基首项、因漏曲压缩后续曲序、下载队列序号

**目录快照**：
四来源抓取批次中某一个已选来源形成的歌手专辑目录结果，包含来源、获取时间、全部合法专辑、完整性状态和来源内去重核查。状态为 `complete` 或 `partial` 且最终有合法歌曲的快照可进入生成来源选择，不存在额外的 partial consent；未选抓取来源没有快照。
_Avoid_：永久目录、实时全集

**来源摘要**：
目录快照面向用户的来源级说明，包含歌手身份、状态、计数、完整性和来源级异常，不包含逐专辑明细或专辑选择。
_Avoid_：专辑列表、逐专辑选择

**去重审计记录**：
来源内部实际移除重复项时形成的核查信息；用户可见字段精简为平台、去重类型、歌曲/歌手、一个稳定 ID 或 Provider ID，以及保留/移除专辑名、albumId、曲序，不展示内部快照位置或重复 Provider ID。早期重复只在原始副本已丢失时提供明确标为非精确的标识符回退；四个平台之间不产生此类比较，缺失原始字段不得推断。
_Avoid_：重复数量、快照遍历位置、重复 Provider ID、跨来源去重、把非精确回退当作原始副本

**四来源预览**：
在一个页面中并列展示本批次已抓取的独立目录快照来源摘要，并仅在实际发生来源内去重时展示去重审计记录；展示在一起不表示跨来源合并，未选来源不显示伪造空结果。
_Avoid_：合并后的全网快照、单一成功状态

**完整性报告**：
对目录快照中的分页、专辑数量、歌曲数量、缺失项和异常项所作的可核验说明。
_Avoid_：成功提示、下载结果

**来源歌单（汇总歌单）**：
用户执行来源歌单批量创建后形成的普通洛雪本地歌单，包含对应来源全部合法专辑经稳定歌曲 ID 去重后的歌曲，不与在线平台歌单建立同步关系。非空 `partial` 不要求逐来源再次确认；只为最终生成来源子集创建“`<输入歌手名>-<酷狗|qq|网易|酷我>-<N>首`”歌单，其中 `N` 等于该来源最终去重后实际提交歌曲数。预览、同名检查、实际创建和残留报告使用同一名称；0 首来源不可进入生成子集。
_Avoid_：跨来源合并歌单、平台同步歌单、下载任务

**生成来源选择**：
抓取结束后，从当前批次状态为 `complete` 或非空 `partial` 的快照中选择的非空来源子集；初始默认选中全部合格来源。`failed`、`cancelled` 或 0 首来源不可选，切换选择不请求 Provider。
_Avoid_：partial consent、未抓取来源、零来源写入、切换选择重新抓取

**四歌单批量写入**：
将当前非空生成来源子集一次写入对应新歌单的操作；目标为选中子集全有或全无，任一创建或加歌失败都尽力删除本次已创建的选中歌单，回滚失败必须报告残留。名称中的“四歌单”表示最多四个平台，不要求每次固定创建四个。
_Avoid_：数据库事务、静默部分成功、按名称删除旧歌单

**歌曲整理根目录**：
用户为本地歌曲整理选择的父目录；其直接子目录按歌手文件夹解释，歌手文件夹内部再递归识别专辑和音频文件。
_Avoid_：下载根目录、单个专辑目录

**MP3 转换输出歌手目录**：
FLAC 转 MP3 在默认来源同级或用户指定输出父目录下，为一个来源歌手维护的唯一输出目录。基础名称先去掉来源末尾的中英文歌曲数后缀再追加 ` MP3`，转换终态名称为“`<来源基础名> MP3（N首）`”；`N` 是终态递归统计的输出普通 MP3 实际数量，部分失败不改变该计数口径且异常仍单独保留。重复运行只复用唯一合法的无数量或带数量候选；多个候选、目标冲突或重解析点均阻断，来源歌手目录和输出父目录不改名。
_Avoid_：来源预估数、本次成功数、多个输出目录自动合并、改名来源歌手目录

**可播放文件**：
受支持音频格式能够从头到尾完成解码检查的本地文件；只读取扩展名、文件头或元数据不能证明可播放。
_Avoid_：格式看似正确、能打开开头

**可安全规范化的 FLAC 尾随数据**：
FLAC 的容器与 STREAMINFO 合法、从首帧到最终音频帧可解码的样本总数与声明完全一致、最终帧结构和 CRC 有效，且剩余异常字节全部位于最终帧之后并处于明确小范围上限内；只允许删除该精确尾随范围，并须对结果重新严格完整解码后发布。
_Avoid_：忽略所有 FFmpeg 错误、按时长猜测截断、中段损坏、样本数不一致、CRC 错误、任意长度尾部

**整理计划**：
基于当前快速统计与同一版本的有效深度验证快照生成的单歌手操作计划；先列出并确认清理范围，再按清理后的真实数量执行专辑和歌手重命名。计划本身不修改磁盘。
_Avoid_：自动整理、仅凭快速统计整理、分离的清理和重命名任务

**稳定物理文件身份**：
歌曲整理在同一次扫描内判断硬链接与唯一歌曲数量所使用的卷标识和文件标识组合；Windows 64 位标识必须从读取起无损保存，不得先经过 JavaScript `number` 舍入。精确身份不同的普通文件分别计数，只有精确身份相同的真实硬链接才去重。
_Avoid_：路径相同、内容哈希、舍入后的文件 ID

**歌曲整理异常详情**：
用户点击歌手行“查看详情”后看到的核查信息，只包含当前快速统计/检查阻塞、可定位异常和同一歌手当前会话最近一次整理异常；不重复歌手汇总、歌手目标名称、逐专辑正常信息或成功步骤。整理后的轻量刷新保留操作异常，同一歌手下一次操作、根目录变化或应用重启清除；全部为空时只显示一句简洁提示。整理的清理确认和操作终态结果仍按流程展示完整目标或结果。
_Avoid_：完整专辑预览、正常整理日志、无反馈空弹窗

**歌曲整理操作进度**：
用户主动发起整理后，由 Main 按当前 operationId 与歌手路径广播的清理、重新计数、重命名、引用同步等真实磁盘操作阶段、已完成步骤数、总步骤数和当前相对目标；回滚作为独立阶段。它不等同于快速统计或检查进度，也不允许用动画或估算百分比伪装真实步骤。
_Avoid_：固定扫描状态、伪造百分比、旧任务进度

**歌曲整理操作异常**：
整理的清理、重命名、引用同步或操作日志终态中的失败、回滚失败和具有异常原因的跳过记录；Renderer 仅在当前根目录会话内按歌手保留最近一次，用于轻量刷新后的人工核查，不写入快速统计/深度验证快照，也不跨重启持久化。
_Avoid_：成功操作历史、跨根目录日志、替代恢复日志

**歌曲整理任务生命周期**：
快速扫描、检查、整理和恢复运行期间均不向用户或应用退出流程提供暂停/取消；相关入口保持禁用，关闭请求只提示等待任务完成。Service 内部中止机制仅用于异常收敛、任务替换和自动化测试。FLAC 转 MP3 的安全取消属于独立模块，不适用于歌曲整理。
_Avoid_：关闭即取消歌曲整理、公开取消扫描、把 FLAC 转换规则套用到歌曲整理
